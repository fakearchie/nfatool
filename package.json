{
  "name": "nfa-pub-tool",
  "version": "0.3.0",
  "private": true,
  "description": "nfa.pub tool — Steam cache login desktop app",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
